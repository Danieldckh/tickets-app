{
  "name": "tickets-app",
  "version": "1.0.0",
  "private": true,
  "description": "ProAgri Tickets admin SPA — SSO-gated, proxies /api/tickets* to Agri360 with shared admin key.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cookie-parser": "^1.4.6",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2"
  }
}
